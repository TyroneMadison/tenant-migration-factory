# Already in the cloud, still single-tenant. Cheapest wave-one candidate.
tenant:
  id: vector-dynamics
  name: Vector Dynamics
  tier: standard
  owner_email: platform-team@example.com
source:
  platform: aws
  os: linux
  region: us-east-1
target:
  cloud: aws
  environment: prod
  region: us-east-1
database:
  engine: mysql
  size_gb: 40
  downtime_budget_minutes: 120
cutover:
  window: weeknight
